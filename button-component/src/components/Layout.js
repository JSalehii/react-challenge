import React from "react";
import Buttons from "./Buttons";
import SideBar from "./SideBar";

const Layout = () => {
    return (
        <>
            <SideBar />
            <Buttons />
        </>
    );
};

export default Layout;
